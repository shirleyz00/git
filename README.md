# 我的学习记录


## GIT 命令
```
mkdir learngit    创建名为learngit的空目录
git init      初始化仓库
git add       将文件放入暂存区域
git commit    提交版本变更
git commit -m "..."    提交版本变更并输入版本信息
git branch    查看当前状态
git branch branch-name    创建分支信息
git status    查看当前状态
git merge     分支合并
git push      上传最新版本
git pull      拉取最新版本
git remote add remote-name remote-url     添加远程仓库

git diff    查看difference
git log     从最近到最远的提交日志查看三次
```